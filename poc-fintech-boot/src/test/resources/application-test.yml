# ============================================================================
# Test profile — Testcontainers dynamically configures datasource and Kafka
# ============================================================================

spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true

logging:
  level:
    com.mariosmant.fintech: DEBUG

